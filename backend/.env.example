# Beispiel-Konfigurationsdatei für das lokale Backend
# Diese Datei kopieren und in `.env` umbenennen.
# Anschließend Werte anpassen (insbesondere Passwörter!).

# --------------------------------------------------
# PostgreSQL
# --------------------------------------------------

POSTGRES_USER=appuser
POSTGRES_PASSWORD=change-me
POSTGRES_DB=appdb

# --------------------------------------------------
# Datenbank-Verbindungs-URL (wird vom API-Service genutzt)
# Format:
# postgresql+psycopg://USER:PASSWORD@HOST:PORT/DBNAME
# --------------------------------------------------

DATABASE_URL=postgresql+psycopg://appuser:change-me@db:5432/appdb

# --------------------------------------------------
# API-Sicherheit
# --------------------------------------------------
# Einfaches gemeinsames Geheimnis für lokale Nutzung.
# Muss vom Client (Desktop-App) im HTTP-Header
# `X-API-Token` mitgesendet werden.

API_TOKEN=token-change-me

# --------------------------------------------------
# CORS (nur relevant für lokale Clients)
# Mehrere Einträge durch Komma trennen.
# --------------------------------------------------

CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,tauri://localhost
