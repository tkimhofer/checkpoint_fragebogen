{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "fs-outbox",
  "description": "Allow writing questionnaire files to Documents",
  "windows": ["main"],
  "permissions": [
    "fs:default",
    "fs:allow-write-text-file",
    "fs:allow-mkdir",
    "fs:allow-document-write",
    "fs:allow-document-read"
  ]
}
